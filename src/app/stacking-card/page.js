import StackingCard from '@/components/StackingCard';

export const metadata = {
  title: 'Stacking Card | Orbit Digital',
  description: 'Stacking Card component animation with GSAP ScrollTrigger in Next.js'
};

export default function StackingCardPage() {
  return <StackingCard />;
}
